# LimitBreak project instructions
Read PROJECT_CONTEXT.md before substantive work and maintain it after material changes.
Only this project is in scope unless the user includes a client or test environment.
Preserve semantic discovery validation, loaded-module ownership, startup-only lifecycle and 320 MiB guards.
Compatibility uses discovered instruction relationships, not client identity or fixed addresses.
Do not deploy, launch clients, change textures or run live stress tests without authorization.
Never track credentials, game data, crash dumps, loaded module captures, SDKs or build output.
Keep environment-specific orchestration, captures and run histories outside the product repository.
